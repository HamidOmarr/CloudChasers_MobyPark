put:
  tags:
  - Users
  summary: Updates a user's identity info (Admin).
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: integer
      format: int64
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/UpdateUserIdentityDto.yaml
      text/json:
        schema:
          $ref: ../../components/schemas/UpdateUserIdentityDto.yaml
      application/*+json:
        schema:
          $ref: ../../components/schemas/UpdateUserIdentityDto.yaml
  responses:
    '200':
      description: Identity updated
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/AdminUserProfileDto.yaml
    '400':
      description: Invalid data
    '401':
      description: Unauthorized
    '404':
      description: User not found
    '500':
      description: Unexpected internal server error
